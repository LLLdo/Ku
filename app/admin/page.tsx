'use client'
import {useEffect,useState} from 'react'
import QRCode from 'qrcode'
export default function Admin(){const [logged,setLogged]=useState(false),[u,setU]=useState('admin'),[p,setP]=useState(''),[remember,setRemember]=useState(true),[rows,setRows]=useState<any[]>([]),[qr,setQr]=useState(''),[err,setErr]=useState('');const load=async()=>{const r=await fetch('/api/registrations',{cache:'no-store'});if(r.status===401){setLogged(false);return}const j=await r.json();setRows(j.rows||[])};useEffect(()=>{if(logged){load();const t=setInterval(load,10000);return()=>clearInterval(t)}},[logged]);const login=async(e:any)=>{e.preventDefault();const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:u,password:p,remember})});if(r.ok){setLogged(true);setErr('')}else setErr('เข้าสู่ระบบไม่สำเร็จ')};const makeQR=async()=>setQr(await QRCode.toDataURL(window.location.origin));if(!logged)return <main className="admin"><div className="card" style={{margin:'8vh auto',maxWidth:460}}><h1 style={{fontFamily:'Prompt'}}>Admin Dashboard</h1><p className="hint">จัดการข้อมูลลงทะเบียนสัมมนา LLLDO 2569</p><form onSubmit={login}><div className="field"><label>Username</label><input value={u} onChange={e=>setU(e.target.value)}/></div><div className="field" style={{marginTop:12}}><label>Password</label><input type="password" value={p} onChange={e=>setP(e.target.value)}/></div><label style={{display:'block',margin:'14px 0'}}><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)}/> จำการเข้าสู่ระบบ 30 วัน</label>{err&&<p className="error">{err}</p>}<button className="btn primary" style={{width:'100%'}}>เข้าสู่ระบบ</button></form></div></main>;return <main className="admin"><div className="topbar"><div><div className="eyebrow">LLLDO • 2569</div><h1 style={{fontFamily:'Prompt',margin:'5px 0'}}>Dashboard</h1><span className="hint">อัปเดตข้อมูลอัตโนมัติทุก 10 วินาที</span></div><button className="btn gold" onClick={makeQR}>สร้าง QR Code</button></div><div className="stats"><div className="stat">ผู้ลงทะเบียน<br/><b>{rows.length}</b> คน</div><div className="stat">มีผู้ติดตาม<br/><b>{rows.filter(r=>r.follower===true||r.follower==='true').length}</b> ราย</div><div className="stat">อาหารเจ/ฮาลาล<br/><b>{rows.filter(r=>['อาหารเจ','อิสลาม (ฮาลาล)'].includes(r.diet)).length}</b> ราย</div><div className="stat">กิจกรรมหลัก<br/><b>{rows.filter(r=>String(r.activity||'').startsWith('เข้าร่วมกิจกรรมและร่วม')).length}</b> ราย</div></div>{qr&&<div className="qr" style={{marginTop:18}}><img src={qr} width={220} alt="QR"/><div>สแกนเพื่อเปิดหน้าลงทะเบียน</div></div>}<div className="tableWrap"><table className="table"><thead><tr><th>เวลา</th><th>ชื่อ</th><th>ฝ่าย</th><th>โทรศัพท์</th><th>ผู้ติดตาม</th><th>อาหาร</th><th>แพ้อาหาร</th><th>กิจกรรม</th><th>กิจกรรมย่อย</th></tr></thead><tbody>{rows.map((r,i)=><tr key={i}><td>{r.timestamp}</td><td>{r.name}</td><td>{r.department}</td><td>{r.phone}</td><td>{r.follower?`${r.count||0} คน`: 'ไม่มี'}</td><td>{r.diet}</td><td>{Array.isArray(r.allergies)?r.allergies.join(', '):r.allergies}</td><td>{r.activity}</td><td>{r.subActivity||r.otherActivity||'-'}</td></tr>)}</tbody></table></div></main>}